apiVersion: apps/v1
kind: Deployment
metadata:
  name: registration-application:1.0.0-6
spec:
  replicas: 2
  selector:
    matchLabels:
      app: registration-application:1.0.0-6
  template:
    metadata:
      labels:
        app: registration-application:1.0.0-6
    spec:
      containers:
        - name: registration-application:1.0.0-6
          image: ngozin/registration-application:1.0.0-6
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080
